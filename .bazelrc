build --cxxopt=-std=c++20
build --host_cxxopt=-std=c++20

# Default image tag; CI overrides with --define version=<release>.
build --define=version=dev

run --platforms=@io_bazel_rules_go//go/toolchain:linux_amd64
